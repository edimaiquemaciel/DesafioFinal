import styled from "styled-components";
import { SectionMixin, TextContent } from "../../globalStyle";

export const SectionSobre = styled.section`
${SectionMixin('row', '#FFFFFF', '0')}
min-height: 80vh;
`

export const TextSobre = styled(TextContent)`
`

