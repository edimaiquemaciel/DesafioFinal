import styled from "styled-components";
import { SectionMixin, TextContent, H2, H3, Paragrafo } from "../../globalStyle";

export const SectionNovidades = styled.section`
${SectionMixin()}
min-height: 100vh;
`

export const TextNovidades = styled(TextContent)`
color: #FFFFFF;
`

